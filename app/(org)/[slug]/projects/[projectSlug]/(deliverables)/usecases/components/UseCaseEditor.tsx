"use client";

/**
 * Kept as a compatibility export for old imports. Generated use cases are read-only; the
 * supported editing surface is the PlantUML source tab.
 */
export function UseCaseEditor() {
  return null;
}
