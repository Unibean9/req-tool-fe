"use client";

/**
 * Kept as a compatibility export for old imports. Actors are extracted from BRD/PRD together
 * with the generated model and are not edited through the use-case screen.
 */
export function UseCaseActorsDialog() {
  return null;
}
